---
title: Give Items & Gold
description: "How to give items and gold between characters at the same map location in Artifacts MMO."
---

import ApiSdkTabs from '../../../components/site/ApiSdkTabs.astro';

Your character can give items or gold to other characters on your account, as long as both characters are on the same map. This feature enables you to specialize some characters as dedicated transporters or traders, making it easier to manage resources within your team.
 
You can use this to optimize logistics or simply transfer gear, resources, or currency to the character who needs them most.

- Both the sender and receiver must be **your own characters**, and they must be located **on the same map**.
- Giving multiple items at once applies a cooldown: **3 seconds per different item given**.

---

### Give Items

To give one or more items to another of your characters, use the following request:

**Endpoint:** `POST /my/{sender}/action/give/item`

<ApiSdkTabs apiUrl="https://api.artifactsmmo.com/docs/#/operations/action_give_items_my__name__action_give_item_post">
  <Fragment slot="api-curl">
  ```http
  curl --request POST \
  --url https://api.artifactsmmo.com/my/{sender}/action/give/item \
  --header 'Accept: application/json' \
  --header 'Authorization: Bearer YOUR_TOKEN' \
  --header 'Content-Type: application/json' \
  --data '{
    "character": "receiverName",
    "items": [
      { "code": "iron_ore", "quantity": 10 },
      { "code": "wood", "quantity": 5 }
    ]
  }'
  ```
  </Fragment>
  <Fragment slot="api-javascript">
  ```js
  const url = 'https://api.artifactsmmo.com/my/{sender}/action/give/item';
const options = {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Accept: 'application/json',
    Authorization: 'Bearer YOUR_TOKEN'
  },
  body: JSON.stringify({
    character: "receiverName",
    items: [
      { code: "iron_ore", quantity: 10 },
      { code: "wood", quantity: 5 }
    ]
  })
};

try {
  const response = await fetch(url, options);
  const data = await response.json();
  console.log(data);
} catch (error) {
  console.error(error);
}
  ```
  </Fragment>
  <Fragment slot="api-python">
  ```python
  import requests

url = "https://api.artifactsmmo.com/my/{sender}/action/give/item"
headers = {
    "Accept": "application/json",
    "Content-Type": "application/json",
    "Authorization": "Bearer YOUR_TOKEN"
}
data = {
    "character": "receiverName",
    "items": [
        {"code": "iron_ore", "quantity": 10},
        {"code": "wood", "quantity": 5}
    ]
}

response = requests.post(url, json=data, headers=headers)
print(response.json())
  ```
  </Fragment>
  <Fragment slot="sdk-python">
  ```python
  from artifacts import ArtifactsClient
from artifacts.models.common import SimpleItemSchema

with ArtifactsClient(token="YOUR_TOKEN") as client:
    char = client.character("{sender}")
    items = [
        SimpleItemSchema(code="iron_ore", quantity=10),
        SimpleItemSchema(code="wood", quantity=5)
    ]
    char.trading.give_items(items=items, character="receiverName")
  ```
  </Fragment>
</ApiSdkTabs>

| Field | Description |
|---|---|
| `character` | Name of the character who will receive the items. |
| `items` | List of items to give (1–20 items). Each item has a `code` and `quantity`. |



---

### Give Gold

To transfer gold to another of your characters on the same map, use the following request:

**Endpoint:** `POST /my/{sender}/action/give/gold`

<ApiSdkTabs apiUrl="https://api.artifactsmmo.com/docs/#/operations/action_give_gold_my__name__action_give_gold_post">
  <Fragment slot="api-curl">
  ```http
  curl --request POST \
  --url https://api.artifactsmmo.com/my/{sender}/action/give/gold \
  --header 'Accept: application/json' \
  --header 'Authorization: Bearer YOUR_TOKEN' \
  --header 'Content-Type: application/json' \
  --data '{
    "character": "receiverName",
    "quantity": 500
  }'
  ```
  </Fragment>
  <Fragment slot="api-javascript">
  ```js
  const url = 'https://api.artifactsmmo.com/my/{sender}/action/give/gold';
const options = {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Accept: 'application/json',
    Authorization: 'Bearer YOUR_TOKEN'
  },
  body: JSON.stringify({
    character: "receiverName",
    quantity: 500
  })
};

try {
  const response = await fetch(url, options);
  const data = await response.json();
  console.log(data);
} catch (error) {
  console.error(error);
}
  ```
  </Fragment>
  <Fragment slot="api-python">
  ```python
  import requests

url = "https://api.artifactsmmo.com/my/{sender}/action/give/gold"
headers = {
    "Accept": "application/json",
    "Content-Type": "application/json",
    "Authorization": "Bearer YOUR_TOKEN"
}
data = {"character": "receiverName", "quantity": 500}

response = requests.post(url, json=data, headers=headers)
print(response.json())
  ```
  </Fragment>
  <Fragment slot="sdk-python">
  ```python
  from artifacts import ArtifactsClient

with ArtifactsClient(token="YOUR_TOKEN") as client:
    char = client.character("{sender}")
    char.trading.give_gold(quantity=500, character="receiverName")
  ```
  </Fragment>
</ApiSdkTabs>

| Field | Description |
|---|---|
| `character` | Name of the character who will receive the gold. |
| `quantity` | Amount of gold to give. |


