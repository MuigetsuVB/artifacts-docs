## Inventory

Each character has 20 slots in his inventory, which can hold a maximum of 100 items. So you can have 20 different items, but a maximum of 100 items in total. At each level, the maximum total items increases by 2.
  
You can view the contents of your inventory with[ this request.](https://api.artifactsmmo.com/docs/#/operations/get_my_characters_my_characters_get)
 
```http filename="JSON" copy
....
"inventory_slot1": "wodden_staff",
"inventory_slot1_quantity": 1,
"inventory_slot2": "gudgeon",
"inventory_slot2_quantity": 5,
"inventory_slot3": "",
"inventory_slot3_quantity": 0,
"inventory_slot4": "",
"inventory_slot4_quantity": 0,
"inventory_slot5": "",
"inventory_slot5_quantity": 0,
"inventory_slot6": "",
"inventory_slot6_quantity": 0,
"inventory_slot7": "",
"inventory_slot7_quantity": 0,
"inventory_slot8": "",
"inventory_slot8_quantity": 0,
"inventory_slot9": "",
"inventory_slot9_quantity": 0,
"inventory_slot10": "",
"inventory_slot10_quantity": 0,
"inventory_slot11": "",
"inventory_slot11_quantity": 0,
"inventory_slot12": "",
"inventory_slot12_quantity": 0,
"inventory_slot13": "",
"inventory_slot13_quantity": 0,
"inventory_slot14": "",
"inventory_slot14_quantity": 0,
"inventory_slot15": "",
"inventory_slot15_quantity": 0,
"inventory_slot16": "",
"inventory_slot16_quantity": 0,
"inventory_slot17": "",
"inventory_slot17_quantity": 0,
"inventory_slot18": "",
"inventory_slot18_quantity": 0,
"inventory_slot19": "",
"inventory_slot19_quantity": 0,
"inventory_slot20": "",
"inventory_slot20_quantity": 0,
"inventory_max_items": 100
```

## Bank

At the bank, you can deposit items and golds. The bank is shared with all your characters, it's the best way to trade with your other characters.

Currently, there is no limit to the number of items you can deposit in your bank.

### Deposit/withdraw item

To deposit or withdraw an item, you must be on a map containing a bank.

You can use this POST request to withdraw an item:

```http filename="cURL" copy
curl --request POST \
  --url https://api.artifactsmmo.com/my/{name}/action/bank/withdraw \
  --header 'Accept: application/json' \
  --header 'Authorization: Bearer INSERT_YOUR_TOKEN_HERE' \
  --header 'Content-Type: application/json' \
  --data '{
  "code": "string",
  "quantity": 1
}'
```
```js filename="Javascript" copy
const url = 'https://api.artifactsmmo.com/my/{name}/action/bank/withdraw';
const options = {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Accept: 'application/json',
    Authorization: 'Bearer INSERT_YOUR_TOKEN_HERE'
  },
  body: '{"code":"string","quantity":1}'
};

try {
  const response = await fetch(url, options);
  const data = await response.json();
  console.log(data);
} catch (error) {
  console.error(error);
}
```
[View the API Reference](https://api.artifactsmmo.com/docs/#/operations/action_withdraw_bank_my__name__action_bank_withdraw_post)


You can use this POST request to deposit an item:

```http filename="cURL" copy
curl --request POST \
  --url https://api.artifactsmmo.com/my/{name}/action/bank/deposit \
  --header 'Accept: application/json' \
  --header 'Authorization: Bearer INSERT_YOUR_TOKEN_HERE' \
  --header 'Content-Type: application/json' \
  --data '{
  "code": "string",
  "quantity": 1
}'
```
```js filename="Javascript" copy
const url = 'https://api.artifactsmmo.com/my/{name}/action/bank/deposit';
const options = {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Accept: 'application/json',
    Authorization: 'Bearer INSERT_YOUR_TOKEN_HERE'
  },
  body: '{"code":"string","quantity":1}'
};

try {
  const response = await fetch(url, options);
  const data = await response.json();
  console.log(data);
} catch (error) {
  console.error(error);
}
```

[View the API Reference](https://api.artifactsmmo.com/docs/#/operations/action_deposit_bank_my__name__action_bank_deposit_post)

### Deposit/withdraw golds

To deposit or withdraw golds, you must be on a map containing a bank.

You can use this POST request to withdraw golds:

```http filename="cURL" copy
curl --request POST \
  --url https://api.artifactsmmo.com/my/{name}/action/bank/withdraw/gold \
  --header 'Accept: application/json' \
  --header 'Authorization: Bearer INSERT_YOUR_TOKEN_HERE' \
  --header 'Content-Type: application/json' \
  --data '{
  "quantity": 1
}'
```
```js filename="Javascript" copy
const url = 'https://api.artifactsmmo.com/my/{name}/action/bank/withdraw/gold';
const options = {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Accept: 'application/json',
    Authorization: 'Bearer INSERT_YOUR_TOKEN_HERE'
  },
  body: '{"quantity":1}'
};

try {
  const response = await fetch(url, options);
  const data = await response.json();
  console.log(data);
} catch (error) {
  console.error(error);
}
```
[View the API Reference](https://api.artifactsmmo.com/docs/#/operations/action_withdraw_bank_gold_my__name__action_bank_withdraw_gold_post)


You can use this POST request to deposit golds:

```http filename="cURL" copy
curl --request POST \
  --url https://api.artifactsmmo.com/my/{name}/action/bank/deposit/gold \
  --header 'Accept: application/json' \
  --header 'Authorization: Bearer INSERT_YOUR_TOKEN_HERE' \
  --header 'Content-Type: application/json' \
  --data '{
  "quantity": 1
}'
```
```js filename="Javascript" copy
const url = 'https://api.artifactsmmo.com/my/{name}/action/bank/deposit/gold';
const options = {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Accept: 'application/json',
    Authorization: 'Bearer INSERT_YOUR_TOKEN_HERE'
  },
  body: '{"quantity":1}'
};

try {
  const response = await fetch(url, options);
  const data = await response.json();
  console.log(data);
} catch (error) {
  console.error(error);
}
```

[View the API Reference](https://api.artifactsmmo.com/docs/#/operations/action_deposit_bank_gold_my__name__action_bank_deposit_gold_post)